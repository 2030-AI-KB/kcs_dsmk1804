1. 커밋(commit)(반영하다): 변경사항을 "기록" 하는 행위
2. push(밀어 넣는다): 로컬저장소(내 컴퓨터) → 원격저장소(git, 서버에 있는 저장소)
3. 반대로 원격 → 로컬은 pull (당겨 받는다)
4. branch("나뭇가지") :  버전

5. 리포지토리(repository) 생성시 자동으로  main 버전이 생성될 것이다. 
=> 일단 신버전의 파일은 git에 밀어넣고 (브랜치 생성)
=> 본인 마음에 들면 메인 브렌치에 덮어쓰기 한다